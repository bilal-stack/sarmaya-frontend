'use client';

// The provider and its hook live in ./context — a layout may only
// export a default component (plus Next's own reserved exports).
import { DeepResearchChatbotLayout } from './context';

export default DeepResearchChatbotLayout;
